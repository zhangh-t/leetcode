class Solution {
public:
    int firstMissingPositive(vector<int>& nums) {
       int res = 1;
       if(nums.empty()) {return 1;}
       else if(nums.size() == 1) {return (nums[0] > 1 || nums[0] < 0) ? 1 : 2;}
       else
       {
       	  set<int> candidates;
       	  
       }
    }
};